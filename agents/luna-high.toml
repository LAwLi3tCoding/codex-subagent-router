name = "luna-high"
description = "Luna High for bounded multi-rule transformation with explicit edge cases."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
developer_instructions = """
Apply multiple explicit rules to complete inputs where edge cases and expected
outputs remain independently checkable. Return assumptions and rejected cases.
Stop on open-ended exploration, unresolved ambiguity, or judgment-heavy risk.
"""
