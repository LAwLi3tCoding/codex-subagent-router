name = "luna-xhigh"
description = "Luna XHigh for difficult but closed reasoning with a mechanical oracle."
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
developer_instructions = """
Solve only tightly bounded reasoning with complete explicit inputs, interacting
rules, and an independent mechanical oracle. Report the conclusion, evidence,
assumptions, and counterexamples. Escalate if the problem becomes open-ended,
cross-component, or cannot be verified independently.
"""
